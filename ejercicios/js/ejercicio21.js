export function potencia2 (vector){
    let vector2= new Array;
    for (let i = 0; i < vector.length; i++) {
       
            console.log(vector[i]);
            vector2[i]= (vector[i])*(vector[i]);

    }
    return vector2
}